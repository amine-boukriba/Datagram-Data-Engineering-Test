version: 2

sources:
  - name: public
    schema: public
    tables:
      - name: raw_data
        description: "Raw scraped product data"
        columns:
          - name: pk
            tests: [unique, not_null]
          - name: id
            tests: [not_null]
          - name: scraped_at
            tests: [not_null]